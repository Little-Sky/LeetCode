// O() don't know, std function [Facepalm]
// Faster than 28% - 28% [Facepalm]


class Solution {
public:
    int strStr(string haystack, string needle) {
        return haystack.find(needle);
    }
};
